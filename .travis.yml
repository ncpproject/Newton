dist: bionic

language: java
addons:
  apt:
    packages:
      - gradle
jdk:
  - openjdk14
  
before_install:
 - sudo apt-get update -y
 - sudo apt-get install -y fakeroot
 - chmod +x gradlew
 
script:
 - ./gradlew check
 - ./gradlew jpackage
