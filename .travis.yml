dist: bionic

language: java

jdk:
  - openjdk14
  
os:
  - linux
  - osx  

before_install:
 - sudo apt-get update -y
 - sudo apt-get install -y fakeroot
 - chmod +x gradlew
 
script:
 - eval $MATRIX_EVAL
 - ./gradlew check
 - ./gradlew jpackage
